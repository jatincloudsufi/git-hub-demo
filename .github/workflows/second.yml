name: second-job
on: push
jobs:
    jobs1:
       name: first-job
       runs-on: ubuntu-latest
       steps:
       - name: step one
         uses: actions/checkout@v2
       - name: step two
         run: date  
     
